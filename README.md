# Интеллектуальная система контроля качества производства филамента (AI-QC System)

## 📖 Описание проекта

Данный проект представляет собой прототип **интеллектуальной промышленной системы (IIoT)** для автоматизации контроля качества при производстве филамента для 3D-печати.

Система решает три задачи:
1.  **Предиктивная аналитика:** Предсказание брака на основе параметров оборудования (до того, как брак будет произведен).
2.  **Обеспечение Доступности (Availability):** Автоматическое восстановление данных при отказе датчиков (Self-Healing AI).
3.  **Обеспечение Целостности (Integrity):** Детекция аномалий и защита от подачи ложных данных (Anomaly Detection).

---

## 🏭 Технологический процесс (Филамент)

Производство филамента — это процесс экструзии термопластов (PLA, ABS, PETG).
**Ключевой показатель качества:** Стабильность диаметра нити (стандарт 1.75 мм ± 0.05 мм) и отсутствие овальности.

### Используемые данные (Параметры процесса)
Для обучения модели был сгенерирован синтетический датасет (100,000 записей), имитирующий физику экструзии.

**Входные параметры (Sensors):**
*   `Extruder_Temp_C`: Температура экструдера (влияет на вязкость).
*   `Screw_Speed_RPM`: Скорость вращения шнека (подача пластика).
*   `Melt_Pressure_Bar`: Давление расплава (индикатор засоров/стабильности).
*   `Puller_Speed_M_min`: Скорость тянущего устройства (определяет толщину нити).
*   `Water_Temp_C`: Температура охлаждающей ванны (влияет на овальность).
*   `Material_Moisture_Pct`: Влажность гранул сырья (критический параметр, вызывающий пузыри).
*   `Air_Humidity_Pct`: Влажность воздуха в цеху (внешний шум).

**Целевая переменная:**
*   `Defect_Flag`: 0 — Норма, 1 — Брак.

---

## 🧠 Архитектура Интеллектуальной Системы

Система состоит из трех взаимосвязанных модулей машинного обучения:

### 1. Модуль качества (Predictive Model)
*   **Алгоритм:** Decision Tree / Random Forest.
*   **Функция:** Анализирует текущие настройки станка и предсказывает вероятность выпуска брака.
*   **Метрика:** ROC AUC ~0.85-0.95.

### 2. Модуль доступности (Availability / Self-Healing)
*   **Алгоритм:** **Bayesian Ridge Regression** (Iterative Imputer).
*   **Функция:** "Виртуальный сенсор". Если физический датчик выходит из строя (возвращает NULL или 0), ИИ восстанавливает его показания на основе корреляций с другими датчиками.
*   **Результат:** Система продолжает работать даже при потере 20-30% датчиков.

### 3. Модуль целостности (Integrity / Gatekeeper)
*   **Алгоритм:** **Elliptic Envelope** (Robust Covariance).
*   **Функция:** Строит статистический "конверт" нормального поведения. Блокирует данные, нарушающие физические законы (например, температура 1000°C или скорость -100 RPM), защищая систему от сбоев и атак (spoofing).

---

## 🛡️ Анализ кибербезопасности (CIA Triad)

Проект реализует защиту по классической модели CIA:

### 🟢 Availability (Доступность)
**Реализация:** Модуль **Self-Healing AI**.
*   **Проблема:** Отказ оборудования (DoS датчиков), обрыв связи. Традиционная система упала бы с ошибкой.
*   **Решение:** Использование `BayesianRidge` для многомерной импутации данных. Модель находит скрытые линейные зависимости (например, между Давлением и Температурой) и заполняет пропуски.
*   **Эффективность:** Восстанавливает до 90% точности прогноза при потере критических датчиков.

### 🔵 Integrity (Целостность)
**Реализация:** Модуль **Anomaly Detection**.
*   **Проблема:** Инъекция ложных данных (Spoofing attack) или некорректная работа сенсоров. Подача на вход модели значения `1000°C` может привести к неверному решению.
*   **Решение:** `EllipticEnvelope` проверяет, находятся ли данные внутри допустимого многомерного распределения (расстояние Махаланобиса).
*   **Результат:** Отсечение физически невозможных данных до попадания в управляющий контур.

### 🔒 Confidentiality (Конфиденциальность)
*Хотя программная реализация шифрования не входила в данный скрипт, в промышленном контуре она обеспечивается:*
*   Шифрование канала связи (TLS/SSL) между датчиками и сервером.
*   Ролевая модель доступа (RBAC) к дашборду управления.
*   Обезличивание данных при передаче в облако.

---

## 🚀 Установка и Запуск

### Требования
*   Python 3.8+
*   Библиотеки: `pandas`, `numpy`, `scikit-learn`, `matplotlib`, `seaborn`

### Инструкция
1.  Установите зависимости:
    ```bash
    pip install pandas numpy scikit-learn matplotlib seaborn
    ```
2.  Поместите файл `filament_production_data.csv` в папку с проектом (или сгенерируйте его первым скриптом).
3.  Запустите Jupyter Notebook (`.ipynb`).

---

## 📊 Пример работы системы

При подаче потока данных система выдает один из статусов:

| Статус | Описание | Модуль |
| :--- | :--- | :--- |
| 🟢 **НОРМА** | Процесс стабилен, риск брака минимален. | Random Forest |
| 🟡 **РИСК** | Параметры в пограничной зоне, требуется внимание. | Random Forest (Prob 0.4-0.6) |
| 🔴 **БРАК** | Высокая вероятность дефекта (влажность, перегрев). | Random Forest |
| ⛔ **ОШИБКА** | **Нарушение целостности.** Данные аномальны (напр., T=1000°C). | Elliptic Envelope |
| 🔧 **ВОССТАНОВЛЕНО** | **Обеспечение доступности.** Данные датчика потеряны, но восстановлены ИИ. | Bayesian Ridge |

---

## 👨‍💻 Авторы

*   Разработка модели и архитектуры данных.
*   Анализ отказоустойчивости (Availability).
*   Реализация защиты целостности (Integrity).